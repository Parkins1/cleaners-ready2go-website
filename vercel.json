{
  "version": 2,
  "buildCommand": "npm ci && npm run build",
  "functions": {
    "server/index.ts": {
      "runtime": "nodejs20.x",
      "includeFiles": ["dist/public/**"]
    }
  },
  "routes": [
    { "src": "/(.*)", "dest": "/server/index.ts" }
  ]
}
